spring:
  application:
    name: demo

  datasource:
    url: jdbc:postgresql://localhost:5432/babit
    username: root
    password: root
    driver-class-name: org.postgresql.Driver

  security:
    user:
      name: admin     # ⚠ 테스트용 계정, 배포 전 반드시 제거
      password: 1234  # ⚠ 테스트용 비밀번호

#    jpa:
#      hibernate:
#        ddl-auto: update
#      show-sql: true
#      properties:
#        hibernate:
#          format_sql: true
#          dialect: org.hibernate.dialect.PostgreSQLDialect